syntax = "proto3";

message Frame {
	string name = 1;
}

message Mesh {
	map<string,VectorArray> vertexData = 1;
	repeated uint32 indices = 2;
}

message VectorArray {
	uint32 dim = 1;
	repeated float data = 2;
}